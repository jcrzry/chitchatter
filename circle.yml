test:
    override:
        - python -m tests/unit_tests
        - python -m tests/socketio_tests
        
deployment:
    staging:
        branch: master
        heroku:
            appname: chit-chatter